version: 2
jobs:
  build:
    # parallelism: 3
    docker:
      - image: ajmjohnson/finery-primary:0.0.1
        environment:
          RAILS_ENV: test
      # - image: circleci/ruby:2.1

        # - image: circleci/ruby:2-node
      # - image: ruby:2.2-onbuild
        # environment:
        #   BUNDLE_JOBS: 3
        #   BUNDLE_RETRY: 3
        #   BUNDLE_PATH: vendor/bundle
      #     PGHOST: 127.0.0.1
      #     PGUSER: circleci-demo-ruby
          # RAILS_ENV: test
      # - image: circleci/mysql:5.7.19
      #   environment:
      #     POSTGRES_USER: finery
      #     POSTGRES_DB: rails_blog
      #     POSTGRES_PASSWORD: ""
    steps:
      - checkout

      - run:
          name: Which bundler?
          command: bundle -v

      - run:
          name: Which ruby?
          command: ruby -v

      - run:
          name: Which Node?
          command: node -v

      # - run:
      #     name: Install RVM
      #     command: \curl -sSL https://get.rvm.io | bash
      #
      # - run:
      #     name: RVM?
      #     command: rvm list rubies

      - run:
          name: Update Bundler
          command: gem install bundler

      # - run:
      #     name: Install Node
      #     command: sudo apt-get install nodejs

      # Restore bundle cache
      - restore_cache:
          keys:
            - rails-demo-bundle-v2-{{ checksum "Gemfile.lock" }}
            - rails-demo-bundle-v2-

      - run:
          name: Bundle Install
          command: bundle check || bundle install

      # Store bundle cache
      - save_cache:
          key: rails-demo-bundle-v2-{{ checksum "Gemfile.lock" }}
          paths:
            - vendor/bundle

      # Only necessary if app uses webpacker or yarn in some other way
      # - restore_cache:
      #     keys:
      #       - rails-demo-yarn-{{ checksum "yarn.lock" }}
      #       - rails-demo-yarn-

      # - run:
      #     name: Yarn Install
      #     command: yarn install #--cache-folder ~/.cache/yarn

      # Store yarn / webpacker cache
      # - save_cache:
      #     key: rails-demo-yarn-{{ checksum "yarn.lock" }}
      #     paths:
      #       - ~/.cache/yarn

      # - run:
      #     name: Wait for DB
      #     command: dockerize -wait tcp://localhost:5432 -timeout 1m


      - run:
          name: Database setup
          command: bundle exec rake db:schema:load --

      - type: shell
        command: |
          bundle exec rspec --profile 10 \
                            --format RspecJunitFormatter \
                            --out test_results/rspec.xml \
                            --format progress \
                            $(circleci tests glob "spec/**/*_spec.rb" | circleci tests split --split-by=timings)

      # - run:
      #     name: Bundle Install
      #     command: bundle check || bundle install

      # - type: shell
      #   command: bundle exec rspec spec/models/product_spec.rb
      #   command: bundle exec rspec

      # - type: shell
      #   command: |
      #     bundle exec rspec --profile 10 \
      #                       --format RspecJunitFormatter \
      #                       --out test_results/rspec.xml \
      #                       --format progress \
      #                       $(circleci tests glob "spec/**/*_spec.rb" | circleci tests split --split-by=timings)

      # - run:
      #     name: Bundle Install
      #     command: bundle check || bundle install
      #
      # - type: shell
      #   command: |
      #     bundle exec rspec --profile 10 \
      #                       --format RspecJunitFormatter \
      #                       --out test_results/rspec.xml \
      #                       --format progress \
      #                       $(circleci tests glob "spec/**/*_spec.rb" | circleci tests split --split-by=timings)
